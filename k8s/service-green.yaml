apiVersion: v1
kind: Service
metadata:
  name: nodejs-green-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: nodejs-green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
